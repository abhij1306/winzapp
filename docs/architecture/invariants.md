# Architecture Invariants

Operational canon lives at the repo root:

- [../../INVARIANTS.md](../../INVARIANTS.md)

Do not duplicate the invariant list here. Update the root file so coding agents read the same rules during implementation.
